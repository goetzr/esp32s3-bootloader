1. What routes stdout to the console UART?
	- esp_rom_install_uart_printf vs ets_install_uart_printf
2. What's an example where application code would call a ROM function directly?
3. ESP-IDF has configuration options for libc (i.e.  CONFIG_LIBC_NEWLIB_NANO_FORMAT). Are libc and libstdc++ pre-built or built each time your project is built?
4. What options are available for a new ESP-IDF project when running menuconfig.py?